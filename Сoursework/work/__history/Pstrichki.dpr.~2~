program Pstrichki;

uses
  Vcl.Forms,
  GameUnit in 'GameUnit.pas' {PlayForm};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TPlayForm, PlayForm);
  Application.Run;
end.
